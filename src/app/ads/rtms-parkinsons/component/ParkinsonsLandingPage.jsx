import ParkinsonsHtmlLanding from './ParkinsonsHtmlLanding';

/** Parkinson's ads landing — HTML design + Zoho via RequestAppointment (see gk/wf page data). */
export default function ParkinsonsLandingPage({ data }) {
  return <ParkinsonsHtmlLanding data={data} />;
}
